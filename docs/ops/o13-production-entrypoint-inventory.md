# O13 Production Entrypoint Inventory

Scope: read-only inventory for cutting CallScore production lanes over to `npm run operating:goal` without duplicate execution. No `.env` values read, no services restarted, no network/provider mutations.

## Active entrypoints

| Lane | Current trigger | Current command / target | Operating goal | Duplicate risk | Cutover action |
|---|---|---|---|---|---|
| monitor | systemd `callscore-control-plane-canary.timer` daily ~04:20 | `npm run control-plane:canary` -> `src/scripts/run-control-plane-canary.ts` | `monitor` | Medium: overlaps Hermes `Leaderboard Sentinel` and 6h observability monitors. | Change canary service to bounded `npm run operating:goal -- --goal monitor --mode read_live`; ensure old canary script is implementation-only. |
| monitor | Hermes cron `2571e8396682` `Leaderboard Sentinel` every 6h | agent prompt direct sentinel workflow | `monitor` | High with systemd canary + observability report. | Replace prompt/direct flow with `operating:goal --goal monitor`; keep sentinel code as wrapper implementation. |
| monitor | Hermes cron `ce8f8df11e3e` 6h observability report | agent prompt/status workflow | `monitor` | High with sentinel/canary. | Convert to read-only `operating:goal --goal monitor` or pause after canary is canonical. |
| dispatch_worker_once | systemd `hermes-worker.service` | `docker compose up -d hermes-worker` | `dispatch_worker_once` | High if a separate graph worker polls while existing container also claims jobs. | Do not add a second poller. Modify worker dispatch path so each claimed job executes through graph goal `dispatch_worker_once`. |
| dispatch_worker_once | Docker `crypto-tuber-ranked-hermes-worker-1` | `npm run pipeline:worker -- --worker-id data-pipeline-worker --poll-ms 15000 --no-channel-tasks` | `dispatch_worker_once` | High with `whop-auto-hermes-worker-1` using same command/worker id. | Preserve DB queue; replace internal switch dispatch with LangGraph dispatch wrapper. Validate no parallel legacy dispatcher remains. |
| dispatch_worker_once | Docker `whop-auto-hermes-worker-1` | same `pipeline:worker` data-pipeline-worker command | `dispatch_worker_once` | High existing duplicate-consumer risk. | Classify before changes; do not run a second graph worker. Decide one canonical data worker or make both use same graph dispatch semantics. |
| dispatch_worker_once | Docker `whop-auto-channel-agent-worker-1` | `pipeline:worker -- --worker-id channel-agent-worker --no-pipeline-jobs` | `dispatch_worker_once` for channel-task dispatch if included | Medium: separate channel-task queue, not DB pipeline jobs. | Leave until pipeline job dispatch is cut; if included, route `runChannelTask` through graph without claiming twice. |
| refresh_data | systemd `callscore-daily-pipeline.timer` daily ~03:20 | `npm run pipeline:daily -- --write --read-api-base https://ops-bridge.call-score.com/api/read --transcript-limit 25 --transcript-concurrency 1 --transcript-gap-ms 20000 --limit-creators 250 --limit-videos 10 --since-days 45 --extract-limit 50 --match-limit 500 --match-batch-size 100` | `refresh_data` | High with Hermes core candle/match/score enqueuer jobs. | Add pass-through bounded args to operating CLI/wrapper; then update service ExecStart to `operating:goal --goal refresh_data` preserving limits. |
| refresh_data | Hermes cron `e4f5b44877b3` every 15m | `callscore-cron-candles.sh` -> safe HH scheduler/enqueuer | `refresh_data` | Medium with daily pipeline and worker consumer. | Either route the script through `operating:goal --goal refresh_data` or classify as producer-only and include in refresh_data receipt lineage. |
| refresh_data | Hermes cron `a9fbeabf4299` daily 03:30 | `callscore-cron-match.sh` -> HH scheduler/enqueuer | `refresh_data` | Medium with daily pipeline. | Same as candles; avoid creating duplicate match jobs for same idempotency window. |
| refresh_data | Hermes cron `a94f36600ba2` daily 03:45 | `callscore-cron-scores.sh` -> HH scheduler/enqueuer | `refresh_data` | Medium with daily pipeline compute stage. | Same as match; preserve idempotency key. |
| refresh_data | Hermes cron `d8e212731b34` daily score refresh | agent prompt compute-scores path | `refresh_data` | High with `callscore-core-scores-hh`. | Replace/disable after `refresh_data` canonical path covers compute-scores. |
| produce_video | Hermes cron `ef964a6268de` every 5m | `callscore-video-queue-consumer.sh` -> `src/scripts/video-queue-consumer.ts --max-jobs default` | `produce_video` | High if `operating:goal produce_video` also consumes queue. | Update script to invoke `operating:goal --goal produce_video --max-items 1`; keep `runVideoStage` as implementation. |
| produce_video | Hermes cron `f39440513eb5` daily 08:00 | `callscore-video-scheduler.sh` -> video job scheduler | `produce_video` | Medium with queue consumer; scheduler creates jobs, consumer advances stages. | Route one bounded create/enqueue step through `produce_video` or keep as producer-only with graph receipt lineage. |
| revenue_now | Hermes cron `9c03a6eea969` twice daily | `callscore-genuine-social-packet.sh` plus CMO agent prompt | `revenue_now` | High if direct CMO script and graph both create packets/posts. | Replace cron script/prompt with `operating:goal --goal revenue_now --mode draft_only` first; publish remains gated/provider-proof only. |
| revenue_now | Hermes cron `144c3a9cc860` every 5m | CMO cooldown catch-up watcher | `revenue_now` | Medium: can trigger same CMO lane outside cadence. | Make watcher call graph or only wake canonical revenue_now job; preserve cooldown/idempotency. |
| revenue_now | Hermes cron `8016c746abb5` weekly marketing digest | CMO/Art-of-War agent prompt | `revenue_now` | Medium with twice-daily CMO. | Route to `revenue_now` draft packet; no provider publish without approval/proof. |
| evidence_research | Hermes cron `adf0644c9e8a` weekly | `callscore-creator-growth-scout.sh` | `evidence_research` | Low/Medium with CMO research. | Wrap scout in `operating:goal --goal evidence_research`; keep script as implementation. |
| alerts | no active scheduler found in read-only inventory | `src/scripts/send-queued-alerts.ts` exists | `alerts` | Unknown until active trigger discovered. | Before cutover, confirm whether alerts are inactive; if active elsewhere, route sender through `alerts`. |
| trust_review | no active scheduler found in read-only inventory | `src/scripts/callscore-non-founder-review.ts` exists | `trust_review` | Low until active trigger discovered. | Keep as implementation; add graph entrypoint only when a real trigger/job exists. |
| enqueue API | systemd `callscore-enqueue.service` active | `node --import tsx src/scripts/callscore-enqueue-server.ts` | producer for `dispatch_worker_once` / `refresh_data` | Do not replace with graph consumer; this only queues jobs. | Keep server unless specific producer cutover is required; graph should preserve DB queue semantics. |
| enqueue CLI | scheduler scripts call `src/scripts/callscore-enqueue-job.ts` | enqueues candles/match/scores/workplane jobs | producer for `refresh_data` | Duplicate jobs if idempotency not preserved. | Keep producer semantics; ensure operating wrapper passes same idempotency/source metadata. |

## Phase 2 decision

Canonical cutover should not spawn parallel pollers. For worker/data/video lanes, update the existing active entrypoint or the implementation it calls so the already-active scheduler/container enters LangGraph exactly once, then invokes the existing implementation module underneath.
