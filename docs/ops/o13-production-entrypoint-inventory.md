# O13 Production Entrypoint Inventory

Scope: read-only inventory for cutting CallScore production lanes over to `npm run operating:goal` without duplicate execution. No `.env` values read, no services restarted, no network/provider mutations.

## Active entrypoints

| Lane | Current trigger | Current command / target | Operating goal | Duplicate risk | Cutover action |
|---|---|---|---|---|---|
| monitor | systemd `callscore-control-plane-canary.timer` daily ~04:20 | `npm run operating:goal -- --goal monitor --read-live --max-items 1` -> `run-control-plane-canary.ts` | `monitor` | Cut: active timer enters the operating graph once; canary writes only workflow/artifact/agent_invocation/approval_gate ledger rows and no final business tables. | Verified live systemd start receipt `op-monitor-collect_receipts-3cd531449c7593c7`; artifact reports `workflow_status=completed`, `final_business_tables_mutated=false`. |
| monitor | Hermes cron `2571e8396682` `Leaderboard Sentinel` every 6h | paused duplicate direct sentinel prompt | `monitor` | Neutralized: paused to prevent overlap with canonical systemd monitor canary. | Paused at 2026-06-25T23:33:48+01:00; do not resume without routing through `operating:goal --goal monitor`. |
| monitor | Hermes cron `ce8f8df11e3e` 6h observability report | paused duplicate direct observability prompt | `monitor` | Neutralized: paused to prevent overlap with canonical monitor canary. | Paused at 2026-06-25T23:33:49+01:00; do not resume without routing through `operating:goal --goal monitor` or explicitly classifying as read-only reporting. |
| dispatch_worker_once | systemd `hermes-worker.service` | `docker compose up -d hermes-worker` | `dispatch_worker_once` | Cut: no second graph poller was added; systemd still starts the canonical compose data worker. | Worker image rebuilt/recreated with internal graph dispatch wrapper; proof receipt `op-dispatch_worker_once-collect_receipts-d87a766f7f971e7d` persisted on host. |
| dispatch_worker_once | Docker `crypto-tuber-ranked-hermes-worker-1` | `npm run pipeline:worker -- --worker-id data-pipeline-worker --poll-ms 15000 --no-channel-tasks` | `dispatch_worker_once` | Cut: active container now claims from the existing DB queue and executes each claimed pipeline job through `worker_dispatch_goal_loop`. | Docker proof worker `o13-docker-persisted-dispatch-proof-20260626T011705Z` completed job #2523/run #2595; node receipt `op-dispatch_worker_once-worker_dispatch_goal_loop-449818c93027bcf7`; DB status `succeeded`; active image `sha256:8452571c0ec2dd82e2c2baf8c28c3a1e5927a17774845c56bd7fc2e92de1041c`. |
| dispatch_worker_once | Docker `whop-auto-hermes-worker-1` | removed stopped duplicate data-pipeline worker | `dispatch_worker_once` | Neutralized: duplicate data-pipeline consumer with the same `data-pipeline-worker` id was stopped/removed. | `scripts/start-whop-auto-workers.sh` now starts only `channel-agent-worker` with `--no-deps --no-recreate`; final topology shows no `whop-auto-hermes-worker-1`. |
| dispatch_worker_once | Docker `whop-auto-channel-agent-worker-1` | `pipeline:worker -- --worker-id channel-agent-worker --no-pipeline-jobs` | `dispatch_worker_once` for channel-task dispatch | Cut: separate channel-task queue remains, but claimed channel tasks now route through the same graph node with an already-claimed task fixture; it does not claim pipeline jobs. | Rebuilt/recreated on image `sha256:8452571c0ec2dd82e2c2baf8c28c3a1e5927a17774845c56bd7fc2e92de1041c`; TDD coverage verifies `dispatchClaimedChannelTaskThroughOperatingGraph` and failure routing to `failChannelTask`. |
| refresh_data | systemd `callscore-daily-pipeline.timer` daily ~03:20 | `callscore-daily-pipeline-operating.sh` -> `npm run operating:goal -- --goal refresh_data --mode bounded_write` -> `callscore-daily-pipeline-impl.sh` | `refresh_data` | Cut: existing timer enters the operating graph once, then preserves the old `pipeline:daily --write ...` implementation under the graph. | Verified with bounded dry-run proof receipt `op-refresh_data-collect_receipts-66030fb156febad0`; no pending/running duplicate core jobs after proof. |
| refresh_data | Hermes cron `e4f5b44877b3` every 15m | `callscore-cron-candles.sh` -> `operating:goal --goal refresh_data --mode bounded_write` -> `callscore-enqueue-candles.sh` | `refresh_data` | Cut: same active cron, one graph-wrapped producer, old script preserved as `callscore-cron-candles-impl.sh`. | Verified live job `candle_refresh` #2516 succeeded with idempotency `local-hh-candles:2026-06-26T00-00-00-000Z`; no pending/running duplicates. |
| refresh_data | Hermes cron `a9fbeabf4299` daily 03:30 | `callscore-cron-match.sh` -> `operating:goal --goal refresh_data --mode bounded_write` -> `callscore-enqueue-match.sh` | `refresh_data` | Cut: same active cron, one graph-wrapped producer, old script preserved as `callscore-cron-match-impl.sh`. | Verified live job `match_prices_batch` #2517 succeeded with idempotency `local-hh-match:2026-06-26`; no pending/running duplicates. |
| refresh_data | Hermes cron `a94f36600ba2` daily 03:45 | `callscore-cron-scores.sh` -> `operating:goal --goal refresh_data --mode bounded_write` -> `callscore-enqueue-scores.sh` | `refresh_data` | Cut: same active cron, one graph-wrapped producer, old script preserved as `callscore-cron-scores-impl.sh`. | Verified live job `compute_scores` #2518 succeeded with idempotency `local-hh-scores:2026-06-26`; no pending/running duplicates. |
| refresh_data | Hermes cron `d8e212731b34` daily score refresh | paused duplicate direct agent prompt | `refresh_data` | Neutralized: old direct compute-scores prompt is paused so it cannot race graph-backed `callscore-core-scores operating graph`. | `cronjob pause d8e212731b34` at 2026-06-26T01:21:34+01:00; canonical score producer is `a94f36600ba2`. |
| produce_video | Hermes cron `ef964a6268de` every 5m | `callscore-video-queue-consumer.sh` -> `operating:goal --goal produce_video --read-live --max-items 1` -> `callscore-video-queue-consumer-impl.sh` | `produce_video` | Cut: same active cron consumes at most one queue item through the graph; old consumer is rollback implementation. | Verified receipt `op-produce_video-collect_receipts-343c00bb50d73133`; one plan queue item advanced to audio with mutation flags false and no duplicate queue keys. |
| produce_video | Hermes cron `f39440513eb5` daily 08:00 | `callscore-video-scheduler.sh` -> `operating:goal --goal produce_video --read-live --max-items 1 --video-scheduler-mode enqueue_scheduled` -> idempotent scheduler | `produce_video` | Cut: same active cron enters graph scheduler mode; scheduler skips existing same-day legacy jobs and avoids duplicate deterministic jobs. | Verified production receipt `op-produce_video-collect_receipts-00f6e77337efceac` with `enqueued_count=0`, `skipped_count=1`, queue delta 0, duplicate keys empty. |
| revenue_now | Hermes cron `9c03a6eea969` twice daily | `callscore-genuine-social-packet.sh` plus CMO agent prompt | `revenue_now` | High if direct CMO script and graph both create packets/posts. | Replace cron script/prompt with `operating:goal --goal revenue_now --mode draft_only` first; publish remains gated/provider-proof only. |
| revenue_now | Hermes cron `144c3a9cc860` every 5m | CMO cooldown catch-up watcher | `revenue_now` | Medium: can trigger same CMO lane outside cadence. | Make watcher call graph or only wake canonical revenue_now job; preserve cooldown/idempotency. |
| revenue_now | Hermes cron `8016c746abb5` weekly marketing digest | CMO/Art-of-War agent prompt | `revenue_now` | Medium with twice-daily CMO. | Route to `revenue_now` draft packet; no provider publish without approval/proof. |
| evidence_research | Hermes cron `adf0644c9e8a` weekly | `callscore-creator-growth-scout.sh` | `evidence_research` | Low/Medium with CMO research. | Wrap scout in `operating:goal --goal evidence_research`; keep script as implementation. |
| alerts | no active scheduler found in read-only inventory | `src/scripts/send-queued-alerts.ts` exists | `alerts` | Unknown until active trigger discovered. | Before cutover, confirm whether alerts are inactive; if active elsewhere, route sender through `alerts`. |
| trust_review | no active scheduler found in read-only inventory | `src/scripts/callscore-non-founder-review.ts` exists | `trust_review` | Low until active trigger discovered. | Keep as implementation; add graph entrypoint only when a real trigger/job exists. |
| enqueue API | systemd `callscore-enqueue.service` active | `node --import tsx src/scripts/callscore-enqueue-server.ts` | producer for `dispatch_worker_once` / `refresh_data` | Do not replace with graph consumer; this only queues jobs. | Keep server unless specific producer cutover is required; graph should preserve DB queue semantics. |
| enqueue CLI | scheduler scripts call `src/scripts/callscore-enqueue-job.ts` | enqueues candles/match/scores/workplane jobs | producer for `refresh_data` | Duplicate jobs if idempotency not preserved. | Keep producer semantics; ensure operating wrapper passes same idempotency/source metadata. |

## Phase 2 decision

Canonical cutover should not spawn parallel pollers. For worker/data/video lanes, update the existing active entrypoint or the implementation it calls so the already-active scheduler/container enters LangGraph exactly once, then invokes the existing implementation module underneath.
