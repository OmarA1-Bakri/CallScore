import { PageShell } from "@/components/layout";

export default function Page() {
  return (
    <PageShell className="shell-placeholder">
      <p className="shell-kicker">Signals</p>
      <h1>Theses forming across creators — ranked by conviction, disclosed by evidence.</h1>
      <p className="shell-lede">Phase 1 establishes route chrome and navigation; full data composition lands in later route phases.</p>
      <div className="shell-panel">
        <span className="shell-square" aria-hidden="true" />
        <p>Active consensus chrome will land after primitives and thesis fixtures.</p>
      </div>
    </PageShell>
  );
}
